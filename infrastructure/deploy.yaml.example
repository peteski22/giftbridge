# GiftBridge Deployment Configuration
# ====================================
#
# This file documents non-sensitive CloudFormation parameters for GiftBridge.
# Copy this file and fill in your values before deploying.
#
# SENSITIVE VALUES (API keys, secrets, tokens) should be passed directly to
# CloudFormation via CLI parameters or AWS Secrets Manager - never commit them.
#
# -----------------------------------------------------------------------------

# SCHEDULE
# --------
# How often to run the sync.
# Examples: "rate(1 hour)", "rate(30 minutes)", "cron(0 * * * ? *)"
SCHEDULE_EXPRESSION: "rate(1 hour)"

# BLACKBAUD ENVIRONMENT
# ---------------------
# Your Blackbaud environment identifier.
# Found in: Blackbaud SKY Developer Portal > My applications > Environment ID
BLACKBAUD_ENVIRONMENT_ID: "your-environment-id"

# GIFT DEFAULTS
# -------------
# These values specify where donations are recorded in Raiser's Edge NXT.
#
# HOW TO FIND YOUR RAISER'S EDGE IDs:
#   1. Open Raiser's Edge NXT
#   2. Go to Configuration > Tables
#   3. Find your Fund, Campaign, and Appeal entries
#   4. The ID is shown in the record details

# REQUIRED: Which Fund should receive the donations?
# Example: "41"
GIFT_FUND_ID: "your-fund-id"

# OPTIONAL: Which Campaign should gifts be attributed to?
# Leave empty if you don't use campaigns.
# Example: "1"
GIFT_CAMPAIGN_ID: ""

# OPTIONAL: Which Appeal should gifts be attributed to?
# Leave empty if you don't use appeals.
# Example: "15"
GIFT_APPEAL_ID: ""

# OPTIONAL: What type of gift is this?
# Default is "Donation" - change only if needed.
# Examples: "Donation", "Grant", "Pledge"
GIFT_TYPE: "Donation"
