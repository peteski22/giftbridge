# GiftBridge Configuration
# =========================
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Then deploy with:
#   cd sam
#   source ../.env
#   sam build && sam deploy --stack-name giftbridge --capabilities CAPABILITY_IAM ...
#
# IMPORTANT: Never commit your .env file - it contains secrets!


# =============================================================================
# BLACKBAUD SKY API
# =============================================================================
# You'll need a Blackbaud SKY API application. Create one at:
# https://developer.blackbaud.com/skyapi/
#
# After creating your app, complete the OAuth flow to get a refresh token.

# Client ID from your SKY API application
BLACKBAUD_CLIENT_ID=""

# Client secret from your SKY API application
BLACKBAUD_CLIENT_SECRET=""

# Environment ID - found in SKY API Developer Portal under your application
BLACKBAUD_ENVIRONMENT_ID=""

# OAuth refresh token - obtained after completing the authorization flow
# This token allows GiftBridge to access your Raiser's Edge data
BLACKBAUD_REFRESH_TOKEN=""

# Subscription key - found in your SKY API developer profile
BLACKBAUD_SUBSCRIPTION_KEY=""


# =============================================================================
# FUNDRAISEUP API
# =============================================================================
# Get your API key from your FundraiseUp dashboard:
# Settings > Integrations > API

FUNDRAISEUP_API_KEY=""


# =============================================================================
# GIFT DEFAULTS
# =============================================================================
# These settings control where donations are recorded in Raiser's Edge NXT.
#
# HOW TO FIND YOUR RAISER'S EDGE IDs:
#   1. Open Raiser's Edge NXT
#   2. Go to Configuration > Tables
#   3. Find your Fund, Campaign, or Appeal entries
#   4. The ID is shown in the record details
#

# REQUIRED: Fund ID where all gifts will be recorded
# Example: "41"
GIFT_FUND_ID=""

# OPTIONAL: Campaign ID to attribute gifts to (leave empty if not using)
# Example: "1"
GIFT_CAMPAIGN_ID=""

# OPTIONAL: Appeal ID to attribute gifts to (leave empty if not using)
# Example: "15"
GIFT_APPEAL_ID=""

# Gift type - usually "Donation", but could be "Grant", "Pledge", etc.
GIFT_TYPE="Donation"


# =============================================================================
# SYNC SCHEDULE
# =============================================================================
# How often to check for new donations.
#
# Examples:
#   "rate(1 hour)"      - Every hour
#   "rate(30 minutes)"  - Every 30 minutes
#   "rate(1 day)"       - Once a day
#   "cron(0 9 * * ? *)" - Daily at 9 AM UTC

SCHEDULE_EXPRESSION="rate(1 hour)"
